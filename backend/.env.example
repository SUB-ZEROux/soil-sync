PORT=3000
# Comma-separated frontend origins. Use * only for local experiments.
CORS_ORIGINS=http://localhost:8000,https://sub-zeroux.github.io
DB_FILE=./data/soil-sync.db
UPLOAD_DIR=./uploads
MAX_UPLOAD_MB=5
